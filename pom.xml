<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.jenkins-ci.plugins</groupId>
    <artifactId>plugin</artifactId>
    <version>1.488</version><!-- which version of Jenkins is this plugin built against? -->
  </parent>

  <groupId>com.suryagaddipati.jenkins</groupId>
  <artifactId>slave-utilization-plugin</artifactId>
  <version>1.8-SNAPSHOT</version>
  <packaging>hpi</packaging>
  <name>Slave Utilization Plugin</name>
  <description>Plugin that lets you specify how much of Node Capacity a job wants to use</description>
  <url>https://wiki.jenkins-ci.org/display/JENKINS/Slave+Utilization+Plugin</url>

  <scm>
    <connection>scm:git:ssh://github.com/jenkinsci/slave-utilization-plugin.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/jenkinsci/slave-utilization-plugin.git</developerConnection>
    <url>https://github.com/jenkinsci/slave-utilization-plugin</url>
  </scm>


  <!-- get every artifact through repo.jenkins-ci.org, which proxies all the artifacts that we need -->
  <repositories>
    <repository>
      <id>repo.jenkins-ci.org</id>
      <url>http://repo.jenkins-ci.org/public/</url>
    </repository>
  </repositories>

  <pluginRepositories>
    <pluginRepository>
      <id>repo.jenkins-ci.org</id>
      <url>http://repo.jenkins-ci.org/public/</url>
    </pluginRepository>
  </pluginRepositories>
</project>
